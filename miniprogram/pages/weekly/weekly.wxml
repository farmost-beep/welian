<!-- pages/weekly/weekly.wxml — SDUI 周报页（后端驱动渲染） -->
<view class="container">
  <sdui-renderer components="{{components}}" loading="{{loading}}" error="{{error}}" bind:itemtap="onItemTap" bind:buttontap="onButtonTap"></sdui-renderer>
</view>
