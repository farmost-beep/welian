<!-- pages/monthly/monthly.wxml — SDUI 月报页（后端驱动渲染） -->
<view class="container">
  <sdui-renderer components="{{components}}" loading="{{loading}}" error="{{error}}" bind:buttontap="onButtonTap"></sdui-renderer>
</view>
