<!-- pages/signals/signals.wxml — SDUI 信号页（后端驱动渲染） -->
<view class="container">
  <sdui-renderer components="{{components}}" loading="{{loading}}" error="{{error}}" bind:itemtap="onItemTap"></sdui-renderer>
</view>
